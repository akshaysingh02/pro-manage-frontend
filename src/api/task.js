import axios from "axios";
const backendUrl = "http://localhost:5001";


export const createTask = async() =>{
    try {
        res.json("api")
    } catch (error) {
        console.log(error)
    }
}